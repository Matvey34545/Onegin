#ifndef COMPARISON_ELEMENTS
#define COMPARISON_ELEMENTS

#include <stdlib.h>

bool comparison_unsigned(const void* elem1, const void* elem2, size_t size_element);
bool MyStrcpm(const void *s1, const void *s2, size_t size_element);

#endif
